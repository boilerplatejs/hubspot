{
  "name": "@boilerplatejs/hubspot",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "boilerplate install --bundle $npm_package_name && boilerplate migrate up --bundle $npm_package_name",
    "uninstall": "boilerplate uninstall --bundle $npm_package_name"
  },
  "dependencies": {
    "hubspot": "^2.3.0"
  }
}